# Allele Default Plugin Bootstrap Configuration
# These WASM binaries will be downloaded and loaded on the first run of the engine.

plugins:
  - name: "allele-exchange-polymarket"
    type: "exchange"
    version: "v1.0.0"
    url: "https://github.com/allele-org/allele-exchange-polymarket/releases/download/v1.0.0/plugin.wasm"
    sha256: "EXPECTED_HASH_HERE"

  # Example of a default sensor you might want to ship with
  # - name: "allele-sensor-copilot"
  #   type: "sensor"
  #   version: "v1.0.0"
  #   url: "https://github.com/allele-org/allele-sensor-copilot/releases/download/v1.0.0/plugin.wasm"
  #   sha256: "EXPECTED_HASH_HERE"
